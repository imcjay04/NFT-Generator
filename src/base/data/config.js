module.exports = {
    token: "",    // Change this to your bot token
    prefix: "?",    // change this to your bot prefix that you want
    
    
     embeds: {
		error: {
            color: "#e62937",    // colour of embed if something bad goes on
		},
        warn: {
             color: "#FFAB57"    //Warn colour
        },
		system: {
		color: "#FF9DCE",   //average colour to send an embed and also the footer text
        }
	},
}
